<Page x:Class="Program.ClientsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Program"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ClientsPage">

    <Grid>
        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="LightBlue" Offset="0"/>
                <GradientStop Color="LightGray" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Таблица Клиенты" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="18" Margin="75,10,0,16"/>
        <TextBox Grid.Row="0" Name="TbSearch" Width="275" HorizontalAlignment="Right" TextChanged="TbSearch_TextChanged" Height="40" VerticalAlignment="Top" Margin="0,0,30,0"/>

        <DataGrid x:Name="DataGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Width="*" Header="ФИО" Binding="{Binding fio}"/>
                <DataGridTextColumn Width="*" Header="Возраст" Binding="{Binding age}"/>
                <DataGridTextColumn Width="*" Header="Дата рождения" Binding="{Binding date_of_birth, StringFormat=dd.MM.yyyy}"/>
                <DataGridTextColumn Width="*" Header="Адрес" Binding="{Binding address}"/>

                <DataGridTemplateColumn>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button x:Name="EditButton" Content="Редактировать" Click="EditButtonClick"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2">
            <Button x:Name="AddButton" Content="Добавить" Click="AddButtonClick"/>
            <Button x:Name="DeleteButton" Content="Удалить" Click="DeleteButtonClick"/>
        </StackPanel>
       
    </Grid>
</Page>
